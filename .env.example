# Clerk (get from https://dashboard.clerk.com)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Stripe (get from https://dashboard.stripe.com/apikeys)
# These are set as managed secrets on the app for deploy.
# For local dev, copy values into .env.local
STRIPE_SECRET_KEY=sk_test_
STRIPE_WEBHOOK_SECRET=whsec_
STRIPE_PRO_PRICE_ID=price_
STRIPE_ENTERPRISE_PRICE_ID=price_

# Database (provision via Neon or local Postgres)
# Set as managed secret on the app for deploy.
DATABASE_URL=postgresql://__REDACTED__use_process.env.DATABASE_URL

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
